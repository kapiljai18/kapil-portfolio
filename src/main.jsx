// Full React Portfolio Website Code
// Create a new React app: npx create-react-app portfolio && cd portfolio
// Replace the contents of src folder with the code below
// Run: npm start for development, npm run build for production

// src/index.js
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


